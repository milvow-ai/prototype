---
/**
 * Textarea — multi-line form input with label support.
 */

// === TYPES ===
interface Props {
  name: string;
  id?: string;
  label?: string;
  placeholder?: string;
  required?: boolean;
  rows?: number;
  variant?: 'light' | 'dark';
  class?: string;
}

// === PROPS ===
const {
  name,
  id,
  label,
  placeholder,
  required = false,
  rows = 5,
  variant = 'light',
  class: extraClass = '',
} = Astro.props;

const inputId = id || name;

// === CLASS MAPS ===
const variantMap: Record<string, string> = {
  light: 'bg-white text-ink border-edge-light placeholder:text-ink-light focus:border-orange focus:ring-2 focus:ring-orange/20',
  dark: 'bg-bg-soft text-white border-edge-dark placeholder:text-white-subtle focus:border-orange focus:ring-2 focus:ring-orange/20',
};

// === CLASS STRINGS ===
const cLabel = variant === 'dark'
  ? 'block text-xs font-bold uppercase tracking-wider text-white-muted mb-2'
  : 'block text-xs font-bold uppercase tracking-wider text-ink-muted mb-2';
const cTextarea = 'w-full px-4 py-3 rounded-lg border outline-none transition-all duration-200 text-base resize-y';
const cFinal = cTextarea + ' ' + variantMap[variant] + ' ' + extraClass;
const cReq = 'text-orange';
---

{label && (
  <label for={inputId} class={cLabel}>
    {label}
    {required && <span class={cReq} aria-label="required"> *</span>}
  </label>
)}
<textarea
  id={inputId}
  name={name}
  placeholder={placeholder}
  required={required}
  rows={rows}
  class={cFinal}
></textarea>
